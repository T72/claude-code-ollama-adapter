name: GitHub
version: 0.0.1
schema: v1
mcpServers:
  - name: GitHub
    type: stdio
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-github"
    env:
      GITHUB_TOKEN: "${env:GITHUB_TOKEN}"
